# FlowForge environment variables
# Copy to .env before running

# API server
FLOWFORGE_HOST=0.0.0.0
FLOWFORGE_PORT=8000

# Defaults used by the CLI (all overridable per-run via flags)
FLOWFORGE_DEFAULT_CONCURRENCY=10
FLOWFORGE_DEFAULT_REQUESTS=50
FLOWFORGE_Z_THRESHOLD=2.0
